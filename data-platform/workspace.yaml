# Dagster workspace configuration.
# Tells dagster-webserver and dagster-daemon where to find the code.
load_from:
  - python_module:
      module_name: housingiq_dagster.definitions
      working_directory: /opt/dagster/app
